"""File-transcription projects.

A transcription "project" is a self-contained folder holding a copy of the
source audio, a canonical ``transcript.json`` (segments + words + timestamps +
confidence), and ``meta.json``. Plain-text / subtitle files are derived views,
regenerated on demand from the canonical document.
"""
